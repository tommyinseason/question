import { postCost } from 'question/helpers/post-cost';
import { module, test } from 'qunit';

module('Unit | Helper | post cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postCost([42]);
  assert.ok(result);
});
